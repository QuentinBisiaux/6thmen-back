include:
  - ../../fixtures/templates.yaml

App\Domain\Auth\Entity\User:
  user_{1..5} (extends user):

  user_admin (extends user):
    username: 6thmen
    email: fake@email.fr

App\Domain\Auth\Entity\UserProfile:
  user_profile_{1..5} (extends user_profile):
    user: "@user_<current()>"
  user_profile_admin (extends user_profile):
    user: '@user_admin'