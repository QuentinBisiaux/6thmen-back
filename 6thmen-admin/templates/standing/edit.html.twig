{% extends 'base.html.twig' %}

{% block title %}Edit Standing{% endblock %}

{% block body %}
    <h1 class="font-extrabold text-2xl my-4">Edit Standing</h1>
    <a class="text-zinc-400 underline" href="{{ path('app_standing_index') }}">Back to the list</a>

    <div class="container mx-auto px-4 sm:px-6 lg:px-8">
        {{ include('standing/_form.html.twig', {'button_label': 'Update'}) }}
        {{ include('standing/_delete_form.html.twig') }}
    </div>
{% endblock %}
